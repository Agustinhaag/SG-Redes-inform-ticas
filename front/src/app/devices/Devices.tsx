import ServiceDevice from "@/components/devices/ServiceDevice";
import React from "react";

const Devices: React.FC = () => {
  return (
    <main>
      <div>
        <ServiceDevice />
      </div>
    </main>
  );
};

export default Devices;
